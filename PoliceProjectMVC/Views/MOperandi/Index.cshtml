@model  IEnumerable<PoliceProjectMVC.Models.MOperandi>

@{
    ViewBag.PageTitle = "Modus Operandi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int b = 1;
}

<div class="card card-outline card-primary">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title mb-0">Modus Operandi</h3>
        <div class="card-tools ml-auto">
            <a href="/MOperandi/Create" class="btn btn-primary"><span class="fas fa-plus"></span>&nbsp;&nbsp;Add New</a>
        </div>
    </div>
    <div class="card-body">
        <div class="container-fluid table-responsive">
            <table class="table dtable table-bordered table-striped table-sm text-nowrap" style="width:100%;">
                <thead>
                    <tr>
                        <td>#</td>
                        <td>Major Head Name</td>
                        <td>SubMajor Head Name</td>
                        <td>Name</td>
                        <td>Name (Hi)</td>
                        <td>Description</td>
                        <td>Action</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (PoliceProjectMVC.Models.MOperandi item in Model)
                    {
                        <tr>
                            <td>@(b++)</td>
                            <td>@item.MHead.Name_EN</td>
                            <td>@item.SMHead.Name_EN</td>
                            <td>@item.Name_EN</td>
                            <td>@item.Name_Hi</td>
                            <td>@item.Description</td>
                            <td>
                                <a href="/MOperandi/Edit/@item.Id" class="btn btn-sm mr-2 btn-warning"><i class="fa fa-edit"></i></a>
                                <a href="/MOperandi/Delete/@item.Id" onclick="return confirm('Are You Sure To Delete ?')" class="btn btn-sm btn-danger"><i class="fa fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>