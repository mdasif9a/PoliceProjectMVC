@model  IEnumerable<PoliceProjectMVC.Models.TblHelplineNumber>

@{
    ViewBag.PageTitle = "All Helpline Number";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int b = 1;
}

<div class="card card-outline card-primary">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title mb-0">All Helpline Number</h3>
        <div class="card-tools ml-auto">
            <a href="/TblHelplineNumber/Create" class="btn btn-primary"><span class="fas fa-plus"></span>&nbsp;&nbsp;Add New</a>
        </div>
    </div>
    <div class="card-body">
        <div class="container-fluid table-responsive">
            <table class="table dtable table-bordered table-striped table-sm text-nowrap" style="width:100%;">
                <thead>
                    <tr>
                        <td>#</td>
                        <td>Name (En)</td>
                        <td>Name (Hi)</td>
                        <td>Designation</td>
                        <td>Type</td>
                        <td>Description (En)</td>
                        <td>Description (Hi)</td>
                        <td>Helpline No.</td>
                        <td>Priority</td>
                        <td>Action</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (PoliceProjectMVC.Models.TblHelplineNumber item in Model)
                    {
                        <tr>
                            <td>@(b++)</td>
                            <td>@item.Name_En</td>
                            <td>@item.Name_Hi</td>
                            <td>@item.Designation.Name_En</td>
                            <td>@item.Type</td>
                            <td class="text-wrap">@item.Description_En</td>
                            <td class="text-wrap">@item.Description_Hi</td>
                            <td class="text-wrap">@item.HelplineNumbers</td>
                            <td>@item.Priority</td>
                            <td>
                                <a href="/TblHelplineNumber/Edit/@item.Id" class="btn btn-sm mr-2 btn-warning"><i class="fa fa-edit"></i></a>
                                <a href="/TblHelplineNumber/Delete/@item.Id" onclick="return confirm('Are You Sure To Delete ?')" class="btn btn-sm btn-danger"><i class="fa fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>