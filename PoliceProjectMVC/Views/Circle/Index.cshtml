@model  IEnumerable<PoliceProjectMVC.Models.Circle>

@{
    ViewBag.PageTitle = "Circle Officers";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int b = 1;
}

<div class="card card-outline card-primary">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title mb-0">Circle Officers</h3>
        <div class="card-tools ml-auto">
            <a href="/Circle/Create" class="btn btn-primary"><span class="fas fa-plus"></span>&nbsp;&nbsp;Add New</a>
        </div>
    </div>
    <div class="card-body">
        <div class="container-fluid table-responsive">
            <table class="table dtable table-bordered table-striped table-sm text-nowrap" style="width:100%;">
                <thead>
                    <tr>
                        <td>#</td>
                        <td>Circle Name (En)</td>
                        <td>Circle Name (Hi)</td>
                        <td>Officer Name (En)</td>
                        <td>Officer Name (Hi)</td>
                        <td>Designation</td>
                        <td>Mobile No.</td>
                        <td>Alt Mobile No.</td>
                        <td>Email</td>
                        <td>Image</td>
                        <td>Priority</td>
                        <td>Action</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (PoliceProjectMVC.Models.Circle item in Model)
                    {
                        <tr>
                            <td>@(b++)</td>
                            <td>@item.Name_En</td>
                            <td>@item.Name_Hi</td>
                            <td>@item.OName_En</td>
                            <td>@item.OName_Hi</td>
                            <td>@item.Designation</td>
                            <td>@item.MobileNo</td>
                            <td>@item.AltMobileNo</td>
                            <td>@item.Circle_Email</td>
                            <td>
                                <img src="@item.ImageUrl" width="50" height="50" />
                            </td>
                            <td>@item.Priority</td>
                            <td>
                                <a href="/Circle/Edit/@item.Id" class="btn btn-sm mr-2 btn-warning"><i class="fa fa-edit"></i></a>
                                <a href="/Circle/Delete/@item.Id" onclick="return confirm('Are You Sure To Delete ?')" class="btn btn-sm btn-danger"><i class="fa fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>