@model IEnumerable<PoliceProjectMVC.Models.GrievancePolice>
@{
    ViewBag.PageTitle = "Grievance Female Police";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int b = 1;
}

<div class="card card-outline card-primary">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title mb-0">Grievance Female Police</h3>
        <div class="card-tools ml-auto">
            <a href="/GrievanceFemalePolice/Create" class="btn btn-primary"><span class="fas fa-plus"></span>&nbsp;&nbsp;Add New Grievance Female Police</a>
        </div>
    </div>
    <div class="card-body">
        <div class="container-fluid table-responsive">
            <table class="table dtable table-bordered table-striped table-sm text-nowrap" style="width:100%;">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Employee id</th>
                        <th>Name</th>
                        <th>Mobile No</th>
                        <th>Email</th>
                        <th>Image</th>
                        <th>Description</th>
                        <th>Current Posting</th>
                        <th>Current Employee Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@(b++)</td>
                            <td>@item.EmployeeId</td>
                            <td>@item.Name</td>
                            <td>@item.MobileNo</td>
                            <td>@item.Email</td>
                            <td>
                                @if (!string.IsNullOrEmpty(item.ImageUrl))
                                {
                                    <img src="@item.ImageUrl" width="50" height="50" class="rounded-circle" />
                                }
                                else
                                {
                                    <img src="/Images/noimage.png" width="50" height="50" class="rounded-circle" />
                                }
                            </td>
                            <td>@item.Description</td>
                            <td>@item.CurrentPosting</td>
                            <td>@item.CurrentStatus</td>
                            <td>
                                <a href="/GrievanceFemalePolice/Edit/@item.Id" class="btn btn-sm mr-2 btn-warning"><i class="fa fa-edit"></i></a>
                                <a href="/GrievanceFemalePolice/Delete/@item.Id" onclick="return confirm('Are you sure you want to delete this GrievancePolice?')" class="btn btn-sm btn-danger"><i class="fa fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
