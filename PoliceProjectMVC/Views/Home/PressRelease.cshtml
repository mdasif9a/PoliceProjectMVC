@model IEnumerable<PoliceProjectMVC.Models.PressRelease>
@{
    ViewBag.Title = "Press Release";
    int b = 1;
}

<section class="container my-3 p-0">
    <div class="row g-3">
        <h4 class="sheader mt-3">Press Release</h4>
    </div>

    <div class="row g-3">
        <div class="col-md-12">
            <table class="table table-bordered table-striped table-sm text-nowrap" style="width:100%;">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Date</th>
                        <th>Release By</th>
                        <th>Title</th>
                        <th>View</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@(b++)</td>
                            <td>@item.Date.ToString("dd-MM-yyyy")</td>
                            <td>@item.CreatedBy</td>
                            <td>@item.Title</td>
                            <td>
                                <a href="@item.Link" target="_blank" class="btn btn-link">View</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>
