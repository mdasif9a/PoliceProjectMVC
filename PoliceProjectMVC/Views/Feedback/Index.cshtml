@model  IEnumerable<PoliceProjectMVC.Models.Feedback>

@{
    ViewBag.PageTitle = "All Feedback";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int b = 1;
}

<div class="card card-outline card-primary">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title mb-0">All Feedback</h3>
        @*<div class="card-tools ml-auto">
            <a href="/Feedback/Create" class="btn btn-primary"><span class="fas fa-plus"></span>&nbsp;&nbsp;Add New</a>
        </div>*@
    </div>
    <div class="card-body">
        <div class="container-fluid table-responsive">
            <table class="table dtable table-bordered table-striped table-sm text-nowrap" style="width:100%;">
                <thead>
                    <tr>
                        <td>#</td>
                        <td>Application Name</td>
                        <td>Email</td>
                        <td>Mobile No.</td>
                        <td>Created Date</td>
                        <td>Action</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (PoliceProjectMVC.Models.Feedback item in Model)
                    {
                        <tr>
                            <td>@(b++)</td>
                            <td>@item.ApplicantName</td>
                            <td>@item.Email</td>
                            <td>@item.MobileNo</td>
                            <td>@item.CreatedDate.ToShortDateString()</td>
                            <td>
                                <a href="/Feedback/Details/@item.Id" class="btn btn-sm mr-2 btn-success"><i class="fa fa-eye"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>